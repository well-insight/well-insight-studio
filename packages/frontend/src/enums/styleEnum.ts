// 主题（实际生效的明暗）
export enum ThemeEnum {
  DARK = 'dark',
  LIGHT = 'light',
}

/** 系统主题模式（含跟随系统） */
export enum ThemeModeEnum {
  LIGHT = 'light',
  DARK = 'dark',
  SYSTEM = 'system',
}

// 语言
export enum LangEnum {
  ZH = 'ZH',
  EN = 'EN',
}

// 预览展示
export enum PreviewScaleEnum {
  FIT = 'fit',
  SCROLL_Y = 'scrollY',
  SCROLL_X = 'scrollX',
  FULL = 'full',
}
