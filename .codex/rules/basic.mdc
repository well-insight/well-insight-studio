---
description: 协作与变更基础
alwaysApply: true
---

# Basic

- 需求、状态流、权限边界不清楚时，先确认再改。
- 只改当前任务需要的代码，不做无关重构。
- 改动遵循现有调用链：页面 -> 调用层 -> 接口 -> 服务层 -> 持久层。
- 前端代码放在 `packages/frontend`，后端代码放在 `packages/backend`。
- 有改动就要有验证：能跑命令就跑命令，不能跑就说明原因。
- 未经要求不做破坏性操作，比如强制覆盖、硬重置、批量删除。
- 不提交密钥；新增环境变量要写清用途和示例位置。
- 注释风格遵循 `code-quality.mdc`：短句、讲原因，不写流水账。
