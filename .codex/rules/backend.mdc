---
description: 后端 API 约定
globs:
  - "packages/backend/**/*"
alwaysApply: false
---

# Backend

- 本项目是 `Express + TypeScript + SQLite`，接口前缀统一是 `/api/v1`。
- `routes` 只挂路由和中间件，不写业务展开。
- `controllers` 负责入参校验、状态码和响应；复杂逻辑进 `services` / `models`。
- 未登录或 Token 失效返回 `401`，已登录但无权限返回 `403`。
- 权限以服务端存储和校验为准，不信任前端自报。
- 密钥、JWT Secret 只走环境变量，不写进仓库。
- 入参非法返回 `400`，并明确指出字段或条件。
- SQL 全部参数化，禁止拼接用户输入。
