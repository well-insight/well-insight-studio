---
description: 代码质量、注释风格与安全底线
alwaysApply: true
---

# Code Quality

- 函数只做一件事，过长就拆。
- 命名跟业务走，删掉无用代码和调试输出。
- 入参先校验，错误要指出字段或条件。
- 禁止空 `catch`，要记录日志、转业务错误或重新抛出。
- 状态流转先判断当前状态是否允许。
- 多步写操作要有事务，明确回滚范围。
- 不拼接 SQL，不信任前端自报的权限。
- 日志和响应里不要带密钥、Token、敏感字段。

## 注释

- 注释给人读，只写为什么，不写在做什么。
- 只保留不明显的约束、边界、权衡、兼容原因。
- 临时方案要写清原因和后续处理方式。
- 不写 AI 腔、教程腔、流水账。
- 删除废弃代码时，一并清理失效注释。

```ts
// SQLite 旧库里没有 display_name，缺省时回退 username
const name = row.display_name || row.username

// 与网关 65s keepalive 对齐，避免服务端先断开
server.keepAliveTimeout = 65000
```
