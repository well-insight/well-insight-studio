---
description: 数据库约定
globs:
  - "**/*.{sql}"
  - "packages/backend/src/models/**/*"
  - "packages/backend/src/config/**/*"
alwaysApply: false
---

# Database

- Schema 优先增量变更，不随意改写不可回溯的历史脚本。
- 字段名要表达业务语义，字段类型和前后端约定保持一致。
- 动态 SQL 全部参数化，禁止字符串拼接。
- 软删除查询默认排除已删除数据。
- 新增索引要能说清楚对应的查询路径。
- 外键和唯一约束要和业务规则一致，写入时别绕过。
- 改表或清理数据前先确认能回滚或可恢复。
- SQL 与模型上的注释只写业务含义和特殊约束，不写过程说明。
