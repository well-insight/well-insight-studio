---
description: 测试约定
globs:
  - "**/*.{test,spec}.{ts,tsx,js,jsx}"
  - "**/__tests__/**/*"
  - "**/tests/**/*"
alwaysApply: false
---

# Testing

- 修 bug 至少保留一条回归路径：自动化优先，其次手工步骤。
- 用例命名用“场景 + 预期结果”。
- 断言对准行为和契约，不只断言没报错。
- 用例彼此独立，不共享脏数据。
- 前端重点测校验、提交、错误提示。
- 后端重点测参数、权限、状态、边界值。
- 时间、随机数、网络依赖尽量固定或注入，避免偶发失败。
- 测试注释只说明非直观场景，不复述断言本身。
