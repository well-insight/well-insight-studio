---
description: 前端约定
globs:
  - "packages/frontend/**/*.{vue,ts,tsx,js,jsx,css,scss}"
alwaysApply: false
---

# Frontend

- 优先复用现有组件、样式体系和交互模式，避免平行造轮子。
- 复杂逻辑抽到 composable / hook，模板保持简单。
- 不擅自改后端约定字段名；展示层映射留在 UI 层。
- 新增、编辑、查看三种行为保持一致；只读态不要写请求。
- 样式默认局部作用域；覆盖全局时要说明范围和原因。
- 颜色、字号、图标尽量沿用现有主题变量，不另起一套。
- 组件、模板、样式里的注释同样遵守 `code-quality.mdc`：短句、写原因，不写旁白。
