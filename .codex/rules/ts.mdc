---
description: TypeScript 约定
globs:
  - "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript

- 少用 `any`，不能避免时先收窄类型再使用。
- API 入参和返回值尽量显式声明，不滥用宽泛 `Record`。
- 数值先解析再运算，避免 `NaN` 传下去。
- 异步调用要有错误分支。
- 导出工具函数尽量保持纯函数；有副作用就在命名里体现。
- 不为了消警告乱加类型断言。
- JSDoc / 行内注释只写约束和原因，不写教程。
