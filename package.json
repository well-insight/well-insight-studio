{
  "name": "well-design",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "prepare": "husky",
    "dev": "pnpm --parallel --filter @well-design/api --filter @well-design/web run dev",
    "dev:web": "pnpm --filter @well-design/web dev",
    "dev:api": "pnpm --filter @well-design/api dev",
    "build": "pnpm --filter @well-design/shared --filter @well-design/web --filter @well-design/api run build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@types/node": "^26.2.0",
    "husky": "^9.1.7",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "typescript": "5.9.3"
    }
  }
}
