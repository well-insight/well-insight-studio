---
description: Wex Design 设计系统第一信源。生成或修改 UI 前必读 DESIGN.md 与 design-tokens。
alwaysApply: true
---

# Wex Design 设计系统

1. **先读** 项目根目录 `DESIGN.md`，再读 `docs/components.md`。
2. **只用** `@wex-design/ui` 的 `Wd*` 组件；全局样式 `@wex-design/ui/styles.css`。
3. **颜色/间距/圆角** 使用 `--wd-*` CSS 变量，禁止裸 hex/rgb（见 `scripts/check-raw-colors.mjs`）。
4. **表单选项** 用 `WdSelect` / `WdTreeSelect`；**动作菜单** 用 `WdDropdown`。
5. 不确定 API 时查 [文档站](https://wex-design.github.io/wex-design-ui/) 或 MCP，**禁止臆造 prop**。
6. 根节点用 `WdConfigProvider`；后台布局用 `WdLayout` 系列。

Token 参考：`design-tokens/tokens.json`
