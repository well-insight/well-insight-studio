---
description: 协作与变更基线
alwaysApply: true
---

# Basic

- 需求、状态流转、权限边界不清楚时，先确认再改。
- 只改完成任务需要的代码，不做无关重构。
- 改之前顺着调用链读：页面/调用方 → 接口 → 服务 → 持久层。
- 前端放 `packages/frontend`，后端放 `packages/backend`。
- 有改动就要有验证：可跑命令、手测步骤，或说明无法验证的原因。
- 未经要求不做破坏性操作（强制覆盖、硬重置等）。
- 不提交密钥；新增环境变量写清用途与示例位置。
- 代码里的注释风格见 `code-quality.mdc`（简短、不复述、去 AI 腔）。
