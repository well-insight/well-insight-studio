---
description: 测试约定
globs:
  - "**/*.{test,spec}.{ts,tsx,js,jsx}"
  - "**/__tests__/**/*"
  - "**/tests/**/*"
alwaysApply: false
---

# Testing

- 修 bug 至少留一条回归路径：优先自动化，其次写清手测步骤。
- 用例命名：场景 + 预期结果。
- 断言对准行为与契约，不只断言「没报错」。
- 用例互不依赖执行顺序，不留下脏数据。
- 前端重点：校验、提交、错误提示。
- 后端重点：参数、权限、状态、事务、边界值。
- 时间/随机/网络要固定或注入，避免偶发失败；不用 `sleep` 硬等。
- 测试里的注释只说明非直观场景，不复述断言内容。
