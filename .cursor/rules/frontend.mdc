---
description: 前端约定
globs:
  - "packages/frontend/**/*.{vue,ts,tsx,js,jsx,css,scss}"
alwaysApply: false
---

# Frontend

- 优先复用现有组件和设计体系，避免平行造同类交互。
- 复杂逻辑抽到 composable / hook；模板保持简单。
- 不擅自改与后端约定的字段名；展示映射放在 UI 层。
- 提交前做前端校验；错误文案对准具体字段和约束。
- 新增 / 编辑 / 查看三态行为一致；查看态只读，不发写请求。
- 样式默认局部作用域；覆盖全局时注明范围和原因。
- 颜色、字号等跟现有主题变量，不另起一套。
- 组件、模板、样式里的注释同样遵守 `code-quality`：短句、写原因、不旁白。
