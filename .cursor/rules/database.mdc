---
description: 数据库约定
globs:
  - "**/*.{sql}"
  - "packages/backend/src/models/**/*"
  - "packages/backend/src/config/**/*"
alwaysApply: false
---

# Database

- Schema 优先增量变更，不随意改写不可回溯的历史脚本。
- 字段名表达业务语义；数值精度与前后端一致。
- 动态 SQL 全部参数化，禁止拼接用户输入。
- 软删除模型的查询默认排除已删数据。
- 新增索引对应明确查询路径，不盲加。
- 外键/唯一约束与业务规则一致，写入不绕过。
- 改表或清理数据前确认可回滚或可恢复。
- SQL 与模型上的注释只标业务含义和特殊约束，不写过程说明。
