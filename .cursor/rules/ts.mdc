---
description: TypeScript 约定
globs:
  - "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript

- 少用 `any`；拿不准用 `unknown` 再收窄。
- API 入参、返回值声明明确类型，避免宽泛 `Record` 贯穿全链。
- 数值先解析再计算，避免 `NaN` 下传。
- 异步调用要有错误分支。
- 导出工具函数尽量是纯函数；有副作用就在命名上体现。
- 不为消告警乱加类型断言。
- JSDoc / 行内注释写约束与约定即可，不要写成教程。
