---
description: 代码质量、安全与注释风格
alwaysApply: true
---

# Code Quality

- 函数只做一件事；过长就拆。
- 命名跟业务走；删掉无用代码和调试输出。
- 入口处校验参数；错误要指出字段或条件。
- 禁止空 `catch`；要记日志、转业务错误或抛出。
- 状态流转先判断当前是否允许。
- 多步写操作开事务，明确回滚范围。
- 不拼接 SQL；不信任前端自报的权限。
- 日志和响应里不要带密码、Token。

## 代码注释（写代码时必须遵守）

注释给人读，不写说明书，不做旁白。

**要写**

- 非显而易见的「为什么」（取舍、坑、兼容、协议约定）。
- 单位、边界、魔法数含义。
- 临时方案：原因 + 条件/日期；TODO/FIXME 写清要做什么。

**不要写**

- 复述代码在干什么（如「定义变量」「遍历列表」「返回结果」）。
- AI 腔 / 教程腔：冗长开场、自我解释、营销式措辞。
- 装饰性分隔线、表情、大段模块说明书。
- 改完代码却不更新的过时注释。

**废弃代码**

- 不用的代码直接删，不要整段注释留着。
- 排查时临时注释掉的，事了恢复或删除。

```ts
// ❌
// 这里我们创建一个用户对象，用于存储从接口返回的数据，方便后续渲染
const user = await fetchUser(id)

// ❌
// 遍历所有项目并过滤出激活状态的项目
const list = items.filter(i => i.active)

// ✅
// SQLite 在部分旧库无 display_name，缺省时回退 username
const name = row.display_name || row.username

// ✅
// 与网关 65s keepalive 对齐，避免服务端先断开
server.keepAliveTimeout = 65000
```
