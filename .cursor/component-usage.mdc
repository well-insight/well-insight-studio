---
description: Wex Design 组件用法与选型（Vue 业务代码）
globs: src/**/*.vue
alwaysApply: false
---

# 组件用法

## 引入

```ts
import { WdButton, WdInput } from '@wex-design/ui'
// 按需：import { WdButton } from '@wex-design/ui/button'
//        import '@wex-design/ui/button/style'
```

## 必选模式

- 表单：`WdForm` + `WdFormItem` + 字段组件；提交用 `@submit` + `type="submit"`。
- 表格：`WdTable` 的 `headers` + `items` + `row-key`；单元格用 `#item-{value}` 插槽；分页优先用表格内置页脚（`server-options` + `server-items-length` 服务端模式）。
- 浮层：详情/编辑 `WdDialog`；侧栏 `WdDrawer`；删除确认 `WdConfirmDialog`。
- 反馈：操作结果 `WdToast` 或 `WdMessage`。

## Props 约定

- 尺寸：`size="small" | "large"`（默认 medium）。
- 危险：`severity="danger"` 或 `invalid` + `errorMessage`。
- 清除：优先 `clearable`（Select 亦支持 `showClear` 别名）。
- 宽度：表单字段 `fluid`；页面级宽度用 layout CSS + `max-width`。
- **图标按钮**：`icon-only` 时必须用 `icon` / `:icon` 传入图标；默认插槽在 `iconOnly` 下不会渲染。带文字时用 `icon` + `label`，不要往默认插槽塞 Lucide 图标。

## 禁止

- 同一页面混用其他 UI 库。
- 用 `WdDropdown` 代替 Select 做枚举选择。
- 手写 table/modal 替代 `WdTable` / `WdDialog`。

完整清单：`docs/components.md`
