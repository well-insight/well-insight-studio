---
description: 生成 Wex Design 业务页面（列表/表单/仪表盘）时的布局规范
alwaysApply: false
---

# 页面布局（Agent Requested）

生成完整页面时，先确定类型并参考 [Wex Design UI 文档站](https://wex-design.github.io/wex-design-ui/) 中的页面模式。

## 列表页区块顺序

1. `WdLayout` + 可选 `WdLayoutSider`
2. `WdBreadcrumb`
3. 筛选区（`WdSpace` + Input/Select + 查询/重置）
4. 工具栏（标题 + 主操作按钮）
5. `WdTable`
6. `WdPagination`（右对齐）

## 表单页区块顺序

1. `WdBreadcrumb`
2. 标题 + 简短说明
3. `WdForm`（`max-width: ~42rem`）
4. 底栏：保存（primary）+ 取消（secondary）

## 仪表盘区块顺序

1. `WdBreadcrumb` + 页面标题
2. KPI：`WdGrid` + `WdCard`（4 列或响应式）
3. 主区两栏：图表占位 + 最近列表 `WdTable`

## 样式

- 页面背景：`var(--wd-color-surface)`
- 区块间距：`var(--wd-space-4)` / `var(--wd-space-6)`
- 筛选区背景：`color-mix(in srgb, var(--wd-color-border) 25%, transparent)`
